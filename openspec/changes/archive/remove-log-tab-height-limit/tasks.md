## Implementation Tasks

- [x] Remove `max-h-96` from log `<pre>` className in `app/agents/[id]/page.tsx:774` (verification: `rg 'max-h-96' app/agents/` returns no matches)
- [x] Run `npm run typecheck && npm run lint` passes

## Future Work

- None
